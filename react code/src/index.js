import React from "react";
import ReactDOM from "react-dom/client";
import MainPage from "./MainPage";
const root = ReactDOM.createRoot(document.getElementById("play-connect"));
root.render(<MainPage />);
